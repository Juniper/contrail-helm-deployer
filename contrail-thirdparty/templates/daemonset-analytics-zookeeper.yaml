{{- if .Values.manifests.daemonset_analytics_zookeeper }}
{{- $_ := set .Values "images" .Values.global.images }}
{{- $context := . }}
---
apiVersion: extensions/v1beta1
kind: DaemonSet
metadata:
  name: contrail-analytics-zookeeper
spec:
  template:
    metadata:
      labels:
{{ tuple $context "opencontrail" "analytics-zookeeper" | include "helm-toolkit.snippets.kubernetes_metadata_labels" | indent 8 }}
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: {{ .Values.labels.analytics_zookeeper.node_selector_key }}
                operator: In
                values:
                - {{ .Values.labels.analytics_zookeeper.node_selector_value }}
      tolerations:
      - key: node-role.kubernetes.io/master
        operator: Exists
        effect: NoSchedule
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      initContainers:
{{ tuple $context .Values.dependencies.analytics_zookeeper nil | include "helm-toolkit.snippets.kubernetes_entrypoint_init_container" | indent 8 }}
      containers:
      - name: zookeeper
        image: {{ .Values.global.images.tags.zookeeper | quote }}
        imagePullPolicy: {{ default "" .Values.global.images.imagePullPolicy | quote }}
        env:
        - name: NODE_TYPE
          value: database
        envFrom:
        - configMapRef:
            name: contrail-analytics-zookeeper
        readinessProbe:
          exec:
            command: ["/zookeeper_health.sh"]
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          exec:
            command: ["/zookeeper_health.sh"]
          initialDelaySeconds: 10
          periodSeconds: 5
        volumeMounts:
        - mountPath: /data
          name: analytics-zookeeper-data
        - mountPath: /datalog
          name: analytics-zookeeper-datalog
        - mountPath: /zookeeper_health.sh
          name: contrail-thirdparty-bin
          subPath: zookeeper_health.sh
      volumes:
      - name: analytics-zookeeper-data
        hostPath:
          path: /var/lib/analytics_zookeeper_data
      - name: analytics-zookeeper-datalog
        hostPath:
          path: /var/lib/analytics_zookeeper_datalog
      - name: contrail-thirdparty-bin
        configMap:
          name: contrail-thirdparty-bin
          defaultMode: 0555
{{- end }}
